def armNum(n):
    sums = 0
    temp = n
    while temp != 0:
        num = temp%10
        temp = temp//10
        sums = sums + (num*num*num)
    print(sums)
    return(sums == n)
print(armNum(int(input())))        
